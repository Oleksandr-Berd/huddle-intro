import LinksList from "../../components/LinksList/LinksList";

const Footer:React.FC = () => {
    return (
        <footer>
            <LinksList/>
        </footer>
      );
}
 
export default Footer;